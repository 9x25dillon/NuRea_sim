{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Test HRM Matrix Integration",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/matrix_integration.py",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}"
    }
  ]
}